spring:
  datasource:
    password: root123
    username: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/omdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
